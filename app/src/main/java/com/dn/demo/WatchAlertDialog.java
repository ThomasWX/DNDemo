package com.dn.demo;

import android.app.AlertDialog;
import android.content.Context;

public class WatchAlertDialog extends AlertDialog {
    public WatchAlertDialog(Context context) {
        super(context, R.style.WatchDialog);
    }
}
